#include<stdio.h>
/**
 * main - the entry point
 * Return: 0 if no error and non-zero if error is found
 */

int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
